- flash.each do |name, msg|

	-if name=='error' 
		- tag = 'alert'
	-elsif name == 'notice'
		- tag = 'success'
	-else 
		- tag == ''

	-if msg.is_a?(String)
		.alert-box{:id => "flash_#{name}", :class => tag}
			= msg 	
			%a.close{:href=>''} &times;